const { RequestHandler } = require( '../../src/structures/RequestHandler' );

describe("structures/RequestHandler Test Suite", function () {
  test("Can Test?", () => {
    expect(true).toBe(true);
  });
});