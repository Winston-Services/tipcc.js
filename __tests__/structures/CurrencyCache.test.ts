const { CurrencyCache } = require("../../src/structures/CurrencyCache");

describe("structures/CurrencyCache Test Suite", function () {
  test("Can Test?", () => {
    expect(true).toBe(true);
  });
});
